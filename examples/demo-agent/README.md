# Demo agent

```sh
npm install
export ANTHROPIC_API_KEY=your-key
# Or use a gateway: export ANTHROPIC_BASE_URL=https://your-gateway.example
npm run dev
```

The agent is available at `POST /agents/demo/<id>` on port 3583.
